[build]
  command = "npm run build"
  publish = "dist"

# SPA: cualquier ruta directa (ej. /trabajos/5) debe servir index.html,
# si no Netlify devuelve 404 al recargar o entrar directo por la URL.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
